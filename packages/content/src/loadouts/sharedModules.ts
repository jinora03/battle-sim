import type { FighterModuleDefinition } from '../schemas';

/** Modules intentionally available to multiple fighters belong in this catalog. */
export const SHARED_MODULES: readonly FighterModuleDefinition[] = [];
